# Secreto: contiene el PAT (ACCESS_TOKEN). NUNCA se commitea.
.env
.env.*

# Modo --secret: el PAT como file-secret. NUNCA se commitea.
access_token

# Compose generado por deploy.sh (específico de cada máquina).
compose.yaml
compose.yml

# Datos locales del runner / logs.
_work/
*.log
